# Products Microservice

## Dev

1. Clone repository
2. Install dependencies `npm install`
3. Add `.env` file based on `env.template`
4. Run prisma migration `npx prisma migrate dev`
5. Run `npm run start:dev`